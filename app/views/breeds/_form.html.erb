<%= form_for(@breed) do |f| %>
  <% if @breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@breed.errors.count, "error") %> prohibited this breed from being saved:</h2>

      <ul>
      <% @breed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :'Género' %><br>
    <%= f.select :species_id, options_for_select(Species.all.map{|s|[s.name, s.id]}, selected_key = @breed.species_id)%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
