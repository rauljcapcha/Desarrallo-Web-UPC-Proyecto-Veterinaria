<%= form_for(@detail_guide) do |f| %>
  <% if @detail_guide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail_guide.errors.count, "error") %> prohibited this detail_guide from being saved:</h2>

      <ul>
      <% @detail_guide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'N° Guía' %><br>
    <%= f.select :guide_id, options_for_select(Guide.all.map{|s|[s.id, s.id]}, selected_key = @detail_guide.guide_id)%>
  </div>
  <div class="field">
    <%= f.label :'Producto' %><br>
    <%= f.select :product_id, options_for_select(Product.all.map{|s|[s.name, s.id]}, selected_key = @detail_guide.product_id)%>
  </div>
  <div class="field">
    <%= f.label :unitprice %><br>
    <%= f.text_field :unitprice %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.text_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :subtotal %><br>
    <%= f.text_field :subtotal %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
