<p id="notice"><%= notice %></p>

<h1>Listing Guides</h1>

<table>
  <thead>
    <tr>
      <th>N° Guía</th>
      <th>N° Atention</th>
      <th>Service</th>
      <th>Total</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guides.each do |guide| %>
      <tr>
        <td><%= guide.id %></td>
        <td><%= guide.atention.id %></td>
        <td><%= guide.service.name %></td>
        <td><%= guide.total %></td>
        <td><%= link_to 'Show', guide %></td>
        <td><%= link_to 'Edit', edit_guide_path(guide) %></td>
        <td><%= link_to 'Destroy', guide, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guide', new_guide_path %>
