<%= form_for(@service) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'Tipo de Servicio' %><br>
    <%= f.select :service_type_id, options_for_select(ServiceType.all.map{|s|[s.name, s.id]}, selected_key = @service.service_type_id)%>
  </div>  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :saleprice %><br>
    <%= f.text_field :saleprice %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
