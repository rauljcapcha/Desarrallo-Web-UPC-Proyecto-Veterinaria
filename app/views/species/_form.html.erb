<%= form_for @species, :html => { :class => "form-horizontal species" } do |f| %>

    <% if @species.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@species.errors.count, "error") %> prohibited this species from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @species.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@species[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            species_index_path, :class => 'btn btn-default' %>

<% end %>
