<%= form_for(@veterinary_user) do |f| %>
  <% if @veterinary_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@veterinary_user.errors.count, "error") %> prohibited this veterinary_user from being saved:</h2>

      <ul>
      <% @veterinary_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nameuser %><br>
    <%= f.text_field :nameuser %>
  </div>
  <div class="field">
    <%= f.label :passworduser %><br>
    <%= f.text_field :passworduser %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
